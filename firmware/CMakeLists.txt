cmake_minimum_required(VERSION 3.16)

# Set the project name shown in ESP-IDF build output
set(PROJECT_NAME "greenhouse_monitor")

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(greenhouse_monitor)
